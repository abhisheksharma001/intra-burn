name: CI

on:
  pull_request:
    branches: [main, dev]
  push:
    branches: [main, dev]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder CI
        run: |
          echo "Add your build, lint, and test commands here"
          echo "Market standard: install deps -> lint -> test -> build"
